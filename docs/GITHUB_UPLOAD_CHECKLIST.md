# GitHubアップロード前チェックリスト

## ✅ 確認事項

### 1. 機密情報の確認

- [x] `scripts/firebase-service-account.json` が `.gitignore` に含まれている
- [x] `node_modules/` が `.gitignore` に含まれている
- [x] サービスアカウントキーがGitに含まれていないことを確認

### 2. ビルドの確認

- [x] 最新のビルドを実行済み
- [x] `public/` ディレクトリが最新の状態

### 3. コミットすべきファイル

以下のファイルはコミットしてください：

#### 新規追加されたファイル
- `docs/FIREBASE_AUTH_*.md` - Firebase認証関連のドキュメント
- `docs/USER_REGISTRATION_*.md` - ユーザー登録関連のドキュメント
- `scripts/set_firebase_custom_claims.js` - Custom Claims設定スクリプト
- `src/assets/js/firebase-config.js` - Firebase設定ファイル
- `src/assets/js/users.js` - ユーザーデータファイル
- `package.json` - Node.js依存関係
- `package-lock.json` - Node.js依存関係のロックファイル

#### 変更されたファイル
- `.gitignore` - Firebase関連の除外設定を追加
- `src/assets/js/auth.js` - Firebase認証ロジックを追加
- `src/layouts/base.html` - Firebase SDKを追加
- `src/pages/signin.html` - ログイン機能を更新
- `src/pages/signup.html` - 登録機能を更新
- `public/` 内のすべてのファイル（ビルド結果）

### 4. コミットしないファイル

以下のファイルはコミットしないでください：

- `scripts/firebase-service-account.json` - 機密情報（.gitignoreに含まれています）
- `node_modules/` - Node.js依存関係（.gitignoreに含まれています）

## 📝 コミット手順

### ステップ1: 変更を確認

```bash
git status
```

### ステップ2: 変更をステージング

```bash
# すべての変更をステージング
git add .

# または、特定のファイルのみをステージング
git add docs/
git add scripts/set_firebase_custom_claims.js
git add src/
git add public/
git add .gitignore
git add package.json
git add package-lock.json
```

### ステップ3: コミット

```bash
git commit -m "feat: Firebase Authentication統合

- Firebase Authenticationを統合
- ユーザー登録機能を実装（customerロール）
- ログイン機能を実装（Firebase + Custom Claims）
- Custom Claims設定スクリプトを追加
- ドキュメントを追加
"
```

### ステップ4: プッシュ

```bash
git push origin main
```

## ⚠️ 注意事項

1. **サービスアカウントキーは絶対にコミットしない**
   - `scripts/firebase-service-account.json` は機密情報です
   - 既に `.gitignore` に含まれていますが、念のため確認してください

2. **Firebase設定ファイル**
   - `src/assets/js/firebase-config.js` にはFirebase設定が含まれています
   - これは公開されても問題ありません（クライアント側で使用するため）

3. **ユーザーデータ**
   - `src/assets/js/users.js` にはユーザーデータが含まれています
   - パスワードはハッシュ化されていますが、本質的にセキュリティ上の制限があります
   - 本番環境では、Firebase Custom Claimsを使用することを推奨します

## ✅ 完了

すべての確認が完了したら、GitHubにアップロードできます！

